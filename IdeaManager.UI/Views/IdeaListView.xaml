<Page x:Class="IdeaManager.UI.Views.IdeaListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Liste des idées"
      Background="White"
      Height="450"
      Width="800"
      Loaded="Refresh">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Toutes les idées qui ont été rajoutées" TextAlignment="Center" FontSize="26" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>
                <ItemsControl x:Name="IdeasItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#007ACC" TextWrapping="Wrap"/>

                                <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" FontSize="12"  Foreground="Gray" Margin="0,4,0,8"/>

                                <TextBlock Text="{Binding Description}"  TextWrapping="Wrap" FontSize="14" Margin="0,0,0,8"/>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Statut: " FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Status}"/>
                                    <TextBlock Text="   Votes: " FontWeight="SemiBold" Margin="12,0,0,0"/>
                                    <TextBlock Text="{Binding VoteCount}"/>
                                </StackPanel>

                                <TextBlock Text="{Binding Project.Name, TargetNullValue='Projet: (aucun)'}" FontStyle="Italic" FontSize="12" Foreground="DarkGray"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
